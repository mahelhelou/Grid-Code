<?php
    $x = 0;
    $y = 8;

    while ($x <= 7) {
        echo "$x <br>";
        $x++;
    } // will run untill the condition is false

    // DO WHILE: run at least once even if the condition isn't met!
    do {
        echo "$y <br>";
        $y--;
    } while ($y > 0); // stop if $y reached to ZERO
    // change while statement to (10)!